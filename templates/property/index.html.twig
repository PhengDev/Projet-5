{% extends 'base.html.twig' %}
{% block title 'Liste des cartes' %}

{% block body %}
    <div class="text-center text-xs-center page bg-collection pb-2">
        <h1 class="text-warning">Cartes</h1>
        <div class="row mr-4 ml-4 cartes-collections card-home">
            {% for property in properties %}
                <div class="col-md-4">
                    {% include "property/_property.html.twig" %}
                </div>
            {% endfor %}
           
        </div>
        <div class="paginator">
            {{ knp_pagination_render(properties) }}
        </div>
    </div>
     
{% endblock %}
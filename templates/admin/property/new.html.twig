{% extends 'base.html.twig' %}
{% block title 'New' %}

{% block body %}
<div class="container style-link text-center">
        <a class="bg-primary text-white backup" href="{{ path('admin.property.index') }}">Retour sur l'administration'</a>
    </div>
    <div class="container text-center mt-4 ">
        <h1>Ajouter une nouvelle carte</h1>
       {{ include('admin/property/_form.html.twig', {button: 'Ajouter'}) }}
    </div>
    {% endblock %}